<h1>商品詳細</h1>

<%= image_tag @item.get_item_image, width: '20%', height: '20%' %>

<table>
  <tr>
    <th>商品名</th>
    <th><%= @item.name %></th>
  </tr>
  <tr>
    <th>商品説明</th>
    <th><%= @item.introduction %></th>
  </tr>
  <tr>
    <th>ジャンル</th>
    <th><%= @item.genre.name %></th>
  </tr>
  <tr>
    <th>税込み価格<br>(税抜価格)</th>
    <th>後で税込み価格を表示するよ(<%= @item.price %>)</th>
  </tr>
  <tr>
    <th>販売ステータス</th>
    <th><%= @item.is_active ? '販売中' : '販売停止中' %></th>
  </tr>
</table>

<%= link_to "編集する", edit_admin_item_path(@item) %>